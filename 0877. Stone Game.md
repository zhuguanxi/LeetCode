# 877. Stone Game

![Medium](https://img.shields.io/badge/Difficulty-Medium-orange)
![Topics](https://img.shields.io/badge/%F0%9F%8F%B7%EF%B8%8F-Topics-blue)
![Companies](https://img.shields.io/badge/%F0%9F%94%92-Companies-yellow)
![Hint](https://img.shields.io/badge/%F0%9F%92%A1-Hint-lightgrey)

## Problem Link
[Problem](https://leetcode.com/problems/stone-game/description/?envType=daily-question&envId=2026-08-02)

## Problem Description
Alice and Bob play a game with piles of stones. There are an even number of piles arranged in a row, and each pile has a positive integer number of stones piles[i].

The objective of the game is to end with the most stones. The total number of stones across all the piles is odd, so there are no ties.

Alice and Bob take turns, with Alice starting first. Each turn, a player takes the entire pile of stones either from the beginning or from the end of the row. This continues until there are no more piles left, at which point the person with the most stones wins.

Assuming Alice and Bob play optimally, return true if Alice wins the game, or false if Bob wins.

### WAY 1:
* 用 DP 來解
```
class Solution {
public:
    int memo[500][500];
    int solve(const vector<int>& piles, int i, int j) {
        if (i == j) 
            return piles[i];

        if (memo[i][j] != 0) {
            return memo[i][j];
        }

        int pickLeft = piles[i] - solve(piles, i + 1, j);
        int pickRight = piles[j] - solve(piles, i, j - 1);

        return memo[i][j] = max(pickLeft, pickRight);
    }
    bool stoneGame(vector<int>& piles) {
        int n = piles.size();
    
        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < n; ++j) {
                memo[i][j] = 0;
            }
        }

        return solve(piles, 0, n - 1) >= 0;
    }
};
```
* n：piles 的長度 (2 <= piles.length <= 500)
* Time Complexity $O(n^2)$
* Space Complexity $O(n^2)$

### WAY 2:
* 因為 piles 是偶數長度，所以奇數位子跟偶數位子個數一樣多，如果第一步拿奇數位子納第二步只能拿偶數位子，相反也是，所以可以將題目簡化成奇數位子跟偶數位子誰總和比較大，就可以一路控制取勝
* 又因為一定有一邊會勝出，所以只要選勝出的那邊就一定會贏，所以可以直接 return true