# 1464. Maximum Product of Two Elements in an Array

![Easy](https://img.shields.io/badge/Difficulty-Easy-brightgreen)
![Topics](https://img.shields.io/badge/%F0%9F%8F%B7%EF%B8%8F-Topics-blue)
![Companies](https://img.shields.io/badge/%F0%9F%94%92-Companies-yellow)
![Hint](https://img.shields.io/badge/%F0%9F%92%A1-Hint-lightgrey)

## Problem Link
[Problem](https://leetcode.com/problems/maximum-product-of-two-elements-in-an-array/description/?envType=daily-question&envId=2026-07-27)

## Problem Description
Given the array of integers nums, you will choose two different indices i and j of that array. Return the maximum value of (nums[i]-1)*(nums[j]-1).

### WAY 1:
* 直接紀錄兩個最大的數，可以不用 for 迴圈
```
class Solution {
public:
    int maxProduct(vector<int>& nums) {
        int first_max = -1, second_max = -1;
        for (auto num : nums)
        {
            if (num >= first_max)
            {
                second_max = first_max;
                first_max = num;
            }
            else if (num > second_max)
                second_max = num;
        }

        return (first_max-1)*(second_max-1);
    }
};
```
* n：nums 的長度 (2 <= nums.length <= 500)
* Time Complexity $O(n)$
* Space Complexity $O(1)$

