# 628. Maximum Product of Three Numbers

![Easy](https://img.shields.io/badge/Difficulty-Easy-brightgreen)
![Topics](https://img.shields.io/badge/%F0%9F%8F%B7%EF%B8%8F-Topics-blue)
![Companies](https://img.shields.io/badge/%F0%9F%94%92-Companies-yellow)
![Hint](https://img.shields.io/badge/%F0%9F%92%A1-Hint-lightgrey)

## Problem Link
[Problem](https://leetcode.com/problems/maximum-product-of-three-numbers/description/?envType=daily-question&envId=2026-07-26)

## Problem Description
Given an integer array nums, find three numbers whose product is maximum and return the maximum product.

### WAY 1:
* 可以不用 sort 直接紀錄每個數字最後在比大小即可
* 如果 sort 的話，也不用計算有幾個負數，可以直接比乘積的大小

```
class Solution {
public:
    int maximumProduct(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        int n = nums.size();
        int nag_cnt = 0;

        for (int i=0; i<n; i++)
        {
            if (nums[i] < 0) 
                nag_cnt++;
            if (nums[i] >= 0)
                break; 
        }

        int ans = (nums[n-1] * nums[n-2] * nums[n-3]);
        if (nag_cnt >= 2)
            ans = max(ans, (nums[n-1] * nums[0] * nums[1]));

        return ans;
    }
};
```
* n：nums 的長度 (3 <= nums.length <= 10e4)
* Time Complexity $O(nlogn)$
* Space Complexity $O(1)$

