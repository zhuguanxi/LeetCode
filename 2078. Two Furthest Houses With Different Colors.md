# 2078. Two Furthest Houses With Different Colors

![Easy](https://img.shields.io/badge/Difficulty-Easy-brightgreen)
![Topics](https://img.shields.io/badge/%F0%9F%8F%B7%EF%B8%8F-Topics-blue)
![Companies](https://img.shields.io/badge/%F0%9F%94%92-Companies-yellow)
![Hint](https://img.shields.io/badge/%F0%9F%92%A1-Hint-lightgrey)

## Problem Link
[Problem](https://leetcode.com/problems/two-furthest-houses-with-different-colors/description/?envType=daily-question&envId=2026-04-20)

## Problem Description
There are n houses evenly lined up on the street, and each house is beautifully painted. You are given a 0-indexed integer array colors of length n, where colors[i] represents the color of the ith house.

Return the maximum distance between two houses with different colors.

The distance between the ith and jth houses is abs(i - j), where abs(x) is the absolute value of x.

### WAY 1:
```
class Solution {
public:
    int maxDistance(vector<int>& colors) {
        int left_max = 0;
        int right_max  = 0;

        int n = colors.size();
        for (int i=n-1; i>=0; i--)
        {
            int first = colors[0];
            if (first != colors[i])
            {
                left_max = abs(i-0);
                break;
            }   
        }

        for (int i=0; i,n; i++)
        {
            int last = colors[n-1];
            if (last != colors[i])
            {
                right_max = abs(n-1-i);
                break;
            }   
        }

        return max(left_max,  right_max);
    }
};
```
* n：colors 的長度 (2 <= n <= 100)
* Time Complexity $O(n)$
* Space Complexity $O(1)$

