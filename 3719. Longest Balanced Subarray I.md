# 3719. Longest Balanced Subarray I

## Problem Link
[Problem](https://leetcode.com/problems/longest-balanced-subarray-i/description/?envType=daily-question&envId=2026-02-10)

## Problem Description
You are given an integer array nums.

A subarray is called balanced if the number of distinct even numbers in the subarray is equal to the number of distinct odd numbers.

Return the length of the longest balanced subarray.

* SubArray : A subarray is a contiguous non-empty sequence of elements within an array.

### WAY 1:
* TLE 
```
class Solution {
public:
    int longestBalanced(vector<int>& nums) {
        int max_subarray = 0;
        unordered_set<int> table;
        for (int i=0; i<nums.size(); i++)
        {
            int tmp = (nums[i] % 2) ? 1 : -1;
            table.insert(nums[i]);
            for (int j = i+1; j<nums.size(); j++)
            {
                if (table.find(nums[j]) == table.end() && nums[j] % 2 == 0)
                    tmp --;
                else if (table.find(nums[j]) == table.end() && nums[j] % 2 !=0)
                    tmp ++;
                table.insert(nums[j]);
                if (tmp == 0)
                {
                    max_subarray = max((j-i)+1, max_subarray);
                }    
            }
            table.clear();
        }

        return max_subarray;
    }
};
```
* Time Complexity $O(N^2 * logN)$