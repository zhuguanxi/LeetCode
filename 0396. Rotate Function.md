# 396. Rotate Function

![Medium](https://img.shields.io/badge/Difficulty-Medium-orange)
![Topics](https://img.shields.io/badge/%F0%9F%8F%B7%EF%B8%8F-Topics-blue)
![Companies](https://img.shields.io/badge/%F0%9F%94%92-Companies-yellow)
![Hint](https://img.shields.io/badge/%F0%9F%92%A1-Hint-lightgrey)

## Problem Link
[Problem](https://leetcode.com/problems/rotate-function/description/?envType=daily-question&envId=2026-05-01)

## Problem Description
You are given an integer array nums of length n.

Assume arrk to be an array obtained by rotating nums by k positions clock-wise. We define the rotation function F on nums as follow:

* F(k) = 0 * arrk[0] + 1 * arrk[1] + ... + (n - 1) * arrk[n - 1].
Return the maximum value of F(0), F(1), ..., F(n-1).

The test cases are generated so that the answer fits in a 32-bit integer.
![Example]()


### WAY 1:
* 雙層 for 迴圈會 TLE，改用DP
```
class Solution {
public:
    int cal_sum(int x, vector<int>& nums)
    {
        int sum = 0;
        for (int i=0; i<nums.size(); i++)
        {
            sum += i * nums[x];
            x++;
            x%=nums.size();
        }

        return sum;
    }
    int maxRotateFunction(vector<int>& nums) {
        int ans = INT_MIN;
        for (int i=0; i<nums.size(); i++)
        {
            int tmp_sum = cal_sum(i, nums); 
            ans = max(ans, tmp_sum);
        }

        return ans;
    }
};
```
* n：nums 的長度 (1 <= n <= 1e5)
* Time Complexity $O(n^2)$
* Space Complexity $O(1)$

### WAY 2:
```
class Solution {
public:
    int maxRotateFunction(vector<int>& nums) {
        int sum = 0, F=0;
        int n = nums.size();

        for (int i=0; i<n; i++)
        {
            sum += nums[i];
            F += i * nums[i];
        }

        int maxn = F;
        for (int i=1; i<n; i++)
        {
            F = F + sum - nums[n-i] * n;
            maxn = max(F, maxn);
        }

        return maxn;

    }
};
```
* Time Complexity $O(n)$
* Space Complexity $O(1)$
